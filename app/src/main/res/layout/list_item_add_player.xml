<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <data class="AddPlayerListBinding">

        <variable
            name="viewModel"
            type="android.dominion.ui.adapter.PlayerListAdapter.PlayerListItemViewModel" />

    </data>

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:gravity="center_vertical"
        android:orientation="horizontal">

        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_margin="10dp"
            android:text='@{viewModel.name}'
            android:textAlignment="center"
            tools:text="AggressivePlayer1" />

        <Spinner
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginLeft="10dp"
            android:layout_marginRight="10dp"
            android:entries='@{viewModel.playerTypes}'
            android:selectedItemPosition='@={viewModel.selectedPlayerTypeIndex}' />

    </LinearLayout>

</layout>